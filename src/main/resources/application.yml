server:
  port: 9296

spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yogeshnagarro/config-server
          clone-on-start: true